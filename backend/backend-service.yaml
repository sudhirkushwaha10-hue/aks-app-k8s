apiVersion: v1
kind: Service

metadata:
  name: aks-app-backend
  namespace: aks-app

spec:
  type: ClusterIP

  selector:
    app: backend

  ports:
    - name: http
      port: 8000
      targetPort: 8000